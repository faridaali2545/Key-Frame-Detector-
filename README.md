Detecting representative keyframes from videos using two deep learning approaches: a Convolutional Autoencoder (unsupervised, reconstruction-error-based) 
and a ResNet50 + LSTM model (supervised, importance-score-based). 
Trained and evaluated on the TVSum dataset.
